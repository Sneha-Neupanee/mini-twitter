apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: mini-twitter
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: mini-twitter
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app.kubernetes.io/name: postgres
