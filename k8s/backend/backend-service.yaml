apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: mini-twitter
  labels:
    app.kubernetes.io/name: mini-twitter-backend
    app.kubernetes.io/part-of: mini-twitter
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: mini-twitter-backend
